build:
    zig build
    mv zig-out/bin/file_watcher ./watcher

clean:
    rm -rf zig-out watcher .watcher

release:
    lash clean
    zig build --release=fast
    mv zig-out/bin/file_watcher ./watcher


